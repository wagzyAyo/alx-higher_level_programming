#!/usr/bin/python3

def max_integer(my_list=[]):
    if len(my_list) == 0:
        return None
    else:
        highest_int = my_list[0]
        for x in range(len( my_list)):
            if my_list[x] > highest_int
            highest_int = my_list[x]
        return highest_int
